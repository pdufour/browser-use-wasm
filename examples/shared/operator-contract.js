/** Re-export operator DOM contract for examples (mirrors `src/config/operator-ui.ts`). */
export {
  MODEL_SWITCHER_ID,
  MODEL_SWITCHER_TEST_ID,
  MODEL_STATUS_ID,
  PROMPT_INPUT_ID,
  BTN_TASK_ID,
} from 'browser-use-wasm';
